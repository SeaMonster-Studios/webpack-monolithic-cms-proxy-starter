
// Add the following line to your wp-config.php file so that asset paths are relative to the root of the wp-content dir and can be proxied correctly by webpack
define( 'WP_CONTENT_URL', '/wp-content');
